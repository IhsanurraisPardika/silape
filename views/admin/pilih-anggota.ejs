<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <title>
        <%= title %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/images/sp.png" type="image/sp.png">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen flex items-center justify-center">

    <div class="bg-white shadow-lg rounded-xl p-8 w-full max-w-md">
        <div class="text-center mb-6">
            <img src="/images/sp.png" alt="Logo" class="h-16 mx-auto mb-4">
            <h1 class="text-2xl font-bold text-gray-800">Siapa yang menilai?</h1>
            <p class="text-gray-500 text-sm">Pilih anggota tim yang sedang bertugas</p>
        </div>

        <% if (error) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4 text-sm text-center">
                <%= error %>
            </div>
            <% } %>

                <form action="/pilih-anggota" method="POST" class="space-y-4">
                    <div class="space-y-2">
                        <% if (anggotaList && anggotaList.length> 0) { %>
                            <% anggotaList.forEach(agg=> { %>
                                <label
                                    class="flex items-center gap-3 p-4 border rounded-lg cursor-pointer hover:bg-red-50 hover:border-red-500 transition-colors group">
                                    <input type="radio" name="anggotaId" value="<%= agg.id %>"
                                        class="w-5 h-5 text-red-600 focus:ring-red-500 border-gray-300">
                                    <div class="flex flex-col">
                                        <span class="font-bold text-gray-800 group-hover:text-red-700">
                                            <%= agg.nama %>
                                        </span>
                                        <span class="text-xs text-gray-500">Anggota <%= agg.urutan %>
                                                <%= agg.urutan===1 ? '(Ketua)' : '' %></span>
                                    </div>
                                </label>
                                <% }) %>
                                    <% } else { %>
                                        <div class="text-center py-4 text-gray-400">
                                            Tidak ada data anggota.
                                        </div>
                                        <% } %>
                    </div>

                    <button type="submit"
                        class="w-full bg-red-600 hover:bg-red-700 text-white font-bold py-3 rounded-xl transition duration-300 shadow-lg shadow-red-700/30">
                        Lanjut
                    </button>

                    <div class="text-center mt-4">
                        <a href="/logout" class="text-sm text-gray-500 hover:text-red-600 underline">Logout / Ganti
                            Akun</a>
                    </div>
                </form>
    </div>

</body>

</html>