<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form Penilaian</title>

    <script src="https://cdn.tailwindcss.com"></script>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <link rel="icon" href="/images/sp.png" type="image/sp.png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .active-menu {
            background: rgba(0, 0, 0, 0.2);
            border-left: 4px solid white;
        }

        .step-active {
            color: var(--sp-red);
            font-weight: bold;
            border-bottom: 3px solid var(--sp-red);
        }

        input[type=number]::-webkit-inner-spin-button,
        input[type=number]::-webkit-outer-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }

        .criteria-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            z-index: 1000;
            align-items: center;
            justify-content: center;
        }

        .score-indicator {
            display: inline-block;
            width: 12px;
            height: 12px;
            border-radius: 50%;
            margin-right: 10px;
        }

        .section-fade-in {
            animation: fadeIn 0.4s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body class="bg-gray-50 font-sans flex min-h-screen">

    <aside class="w-64 sidebar-gradient text-white flex-shrink-0 fixed h-full shadow-xl">
        <%- include('partials/sidebar') %>
    </aside>

    <div class="flex-1 flex flex-col ml-64">
        <%- include('partials/header') %>

            <main class="p-8">
                <div class="max-w-5xl mx-auto space-y-6">
                    <div class="flex items-center justify-between">
                        <div>
                            <h1 class="text-2xl font-bold text-gray-800">Input Penilaian 5P</h1>
                            <p class="text-gray-500 text-sm mt-1">Silakan isi penilaian standar 5P berdasarkan kondisi
                                aktual di kantor yang dipilih.</p>
                        </div>
                    </div>

                    <div
                        class="bg-white rounded-lg shadow-sm px-6 py-5 flex justify-between items-center border border-gray-100">
                        <div class="flex items-center gap-4">
                            <div class="bg-blue-50 p-3 rounded-lg text-blue-600">
                                <i class="fas fa-building text-xl"></i>
                            </div>
                            <div>
                                <h2 id="office-title" class="font-bold text-gray-700 uppercase tracking-wide">
                                    <%= (kantor?.nama || 'KANTOR PENILAIAN' ).toUpperCase() %>
                                </h2>
                                <p class="text-gray-400 text-xs">Formulir Penilaian Standar 5P Kantor</p>
                            </div>
                        </div>
                        <button type="button" onclick="openOfficeModal()"
                            class="bg-red-600 text-white px-4 py-2 rounded-md text-sm font-bold hover:bg-red-700 transition shadow-sm flex items-center gap-2">
                            <i class="fas fa-exchange-alt text-xs"></i>
                            <span>Ganti Kantor</span>
                        </button>
                    </div>

                    <div
                        class="bg-white rounded-t-xl border-x border-t border-gray-200 p-2 flex justify-between shadow-sm">
                        <button onclick="jumpToStep(1)" id="nav-1"
                            class="flex flex-col items-center flex-1 py-3 transition step-active"><span
                                class="text-[10px] font-bold uppercase">P1 - Pemilahan</span></button>
                        <button onclick="jumpToStep(2)" id="nav-2"
                            class="flex flex-col items-center flex-1 py-3 transition text-gray-400"><span
                                class="text-[10px] font-bold uppercase">P2 - Penataan</span></button>
                        <button onclick="jumpToStep(3)" id="nav-3"
                            class="flex flex-col items-center flex-1 py-3 transition text-gray-400"><span
                                class="text-[10px] font-bold uppercase">P3 - Pembersihan</span></button>
                        <button onclick="jumpToStep(4)" id="nav-4"
                            class="flex flex-col items-center flex-1 py-3 transition text-gray-400"><span
                                class="text-[10px] font-bold uppercase">P4 - Pemantapan</span></button>
                        <button onclick="jumpToStep(5)" id="nav-5"
                            class="flex flex-col items-center flex-1 py-3 transition text-gray-400"><span
                                class="text-[10px] font-bold uppercase">P5 - Pembiasaan</span></button>
                    </div>

                    <div class="bg-white rounded-b-xl shadow-lg border border-gray-200 overflow-hidden">
                        <div
                            class="bg-[#C8102E] text-white px-8 py-4 font-bold uppercase tracking-widest text-sm flex justify-between items-center">
                            <span id="step-title-display">Kriteria P1 - Pemilahan</span>
                            <span class="bg-white/20 px-3 py-1 rounded text-[10px]" id="step-counter">STEP 1 OF 5</span>
                        </div>

                        <form id="penilaian-form">
                            <div id="dynamic-form-content" class="p-8 space-y-12 min-h-[400px]"></div>

                            <div
                                class="p-8 border-t border-gray-50 flex justify-center items-center gap-4 bg-gray-50/50">
                                <button type="button" id="prevBtn" onclick="changeStep(-1)"
                                    class="px-10 py-2.5 bg-gray-200 text-gray-600 rounded-lg font-bold hover:bg-gray-300 transition text-sm">Kembali</button>
                                <button type="button" id="nextBtn" onclick="changeStep(1)"
                                    class="px-12 py-2.5 bg-gray-400 text-white rounded-lg font-bold hover:bg-red-600 shadow-md transition text-sm">Selanjutnya</button>
                            </div>
                        </form>
                    </div>
                </div>
            </main>
    </div>

    <div id="criteria-modal" class="criteria-modal p-4">
        <div class="bg-white rounded-xl max-w-5xl w-full shadow-2xl overflow-hidden section-fade-in">
            <div class="bg-gray-50 px-6 py-4 flex justify-between items-center border-b">
                <h3 id="modal-title" class="font-bold text-gray-700 uppercase text-xs tracking-widest">Penjelasan
                    Kriteria</h3>
                <button onclick="closeModal()"
                    class="text-gray-400 hover:text-red-600 text-2xl transition">&times;</button>
            </div>
            <div id="modal-body" class="p-6 space-y-3 max-h-[70vh] overflow-y-auto font-medium text-gray-600 text-sm">
            </div>
            <div class="p-4 bg-gray-50 text-right border-t">
                <button onclick="closeModal()"
                    class="bg-gray-800 text-white px-6 py-2 rounded-md font-bold text-xs hover:bg-black transition">Tutup</button>
            </div>
        </div>
    </div>

    <div id="office-modal" class="criteria-modal p-4">
        <div class="bg-white rounded-xl max-w-xl w-full shadow-2xl overflow-hidden section-fade-in">
            <div class="bg-gray-50 px-6 py-4 flex justify-between items-center border-b">
                <h3 class="font-bold text-gray-700 uppercase text-xs tracking-widest">
                    Pilih Kantor Penilaian
                </h3>
                <button onclick="closeOfficeModal()"
                    class="text-gray-400 hover:text-red-600 text-2xl transition">&times;</button>
            </div>
            <div class="p-4 space-y-3 max-h-[60vh] overflow-y-auto text-sm">
                <p class="text-gray-500 text-xs mb-2">Klik salah satu kantor berikut untuk mengganti tujuan penilaian.
                </p>
                <div id="office-list" class="space-y-2"></div>
            </div>
            <div class="p-4 bg-gray-50 text-right border-t">
                <button onclick="closeOfficeModal()"
                    class="bg-gray-800 text-white px-6 py-2 rounded-md font-bold text-xs hover:bg-black transition">
                    Tutup
                </button>
            </div>
        </div>
    </div>

    <script id="fp-data" type="application/json"><%- JSON.stringify({
        kantorId: kantor ? kantor.id : null,
        kantorNama: kantor ? kantor.nama : null,
        kantorList: (kantorList || []).map(k => ({ id: k.id, nama: k.nama })),
        userNama: anggotaAktif ? anggotaAktif.nama : (user.nama || user.email),
        existingDetails: existingDetails || [],
        catatanRekomendasi: catatanRekomendasi || ""
    }) %></script>
    <script src="/js/formPenilaian.js"></script>
</body>

</html>