<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Input Penilaian 5P</title>
	<link rel="icon" href="/images/sp.png" type="image/sp.png">
	<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="antialiased bg-gray-50">
	<div class="flex min-h-screen">
		<!-- Sidebar -->
		<%- include('partials/sidebar') %>
	</div>

		<!-- Main -->
		<div class="flex-1 flex flex-col">
			<%- include('partials/header') %>


			<main class="p-8">
				<h1 class="text-3xl font-bold text-gray-900 mb-6">Input Penilaian 5P</h1>

				<div class="bg-white rounded-2xl shadow-lg p-6">
					<h2 class="text-xl font-semibold text-gray-800 mb-4">Pilih Kantor yang akan Dinilai</h2>

					<div class="space-y-4">
						<% if (kantorList && kantorList.length) { %>
							<% kantorList.forEach((k) => { %>
								<a href="/formPenilaian?kantor=<%= k.id %>" class="flex items-center justify-between bg-white border border-gray-200 rounded-xl p-4 hover:shadow-md transition">
									<div class="flex items-center gap-4">
										<div class="w-12 h-12 rounded-lg bg-red-50 flex items-center justify-center text-red-600">
											<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
												<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7h18M3 12h18M7 17h10" />
											</svg>
										</div>
										<div>
											<div class="text-sm font-semibold text-gray-800"><%= (k.nama || '').toUpperCase() %></div>
											<% if (k.timNama) { %>
												<div class="text-xs text-gray-400 mt-1"><%= k.timNama %></div>
											<% } %>
										</div>
									</div>
									<div class="text-gray-300">
										<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
											<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
										</svg>
									</div>
								</a>
							<% }) %>
						<% } else { %>
							<div class="p-4 rounded-xl border border-dashed border-gray-300 bg-gray-50 text-sm text-gray-600">
								Tidak ada kantor yang ditugaskan untuk tim Anda (atau data kantor belum tersedia).
							</div>
						<% } %>

					</div>
				</div>
			</main>
		</div>
	</div>
</body>
</html>
